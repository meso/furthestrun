{
  "scripts": {
    "build": "npx tsc",
    "clean": "npx tsc --build --clean"
  },
  "dependencies": {
    "@minecraft/server": "^1.7.0",
    "@minecraft/server-ui": "^1.1.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
